# using a for loop, make a program that will multiply every number
# in the my_list variable by 3 and print the result
# Note: you cannot change the variable my_list. You can do
# everything else though

my_list = int([ 2, 4, 6, 8])
print(mylist[0]*3)
