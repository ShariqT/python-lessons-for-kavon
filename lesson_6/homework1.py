# For some reason this program doesn't actually add the numbers
# it just prints both of them out. Make it so that 
# when I enter in a 3, it will print out 23 instead of 320

user_input = input("Pick a number more than 1: ")
if str(user_input) > "1":
    print(3 + 20)


