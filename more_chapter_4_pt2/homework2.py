# make a new function named 'create_db_entry' that will take two parameters
# that will be called new_entry and db. This new function should append the
# 'new_entry' parameter into the 'db' variable and then return the db.

# This function will basically save our entries in the password variable